# Escape a literal `$` inside a double-quoted value so it survives parsers
# that would otherwise interpret bare `$1` as an interpolation reference.
PRICE="\$1.25"

# Same mechanism for a literal `${...}` sequence that must not be expanded.
LITERAL_REF="\${HOME}"
