# Since :? fails when the variable is missing, the referenced keys must be set.
# The error message is not checked here - we only verify a present value passes
# through and that ${VAR} is evaluated.
VAR=value
REQUIRED=${VAR:?}
REQUIRED_WITH_MSG="${VAR:?VAR does not exist}"
